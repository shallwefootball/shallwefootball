      <% if(user) { %>
      <% }else {%>
        <% joinedClub.forEach(function(item) { %>
          <div class="row marketing placeholders">
            <div class="col-lg-4 placeholder">
              <a data-toggle="modal" data-target="#clubId<%=item.clubId%>">
                <img class="img-circle" src="<%= item.clubLogoImageUrl %>" width="140" height="140" class="img-circle">
              </a>
              <a data-toggle="modal" data-target="#clubId<%=item.clubId%>">
                <h4><%= item.clubName %></h4>
              </a>
              <% include clubModal %>
            </div><!-- /.col-lg-4 -->
          </div>
        <% }) %>
      <% }%>