
			<%	if (league.status == 'before') { %>
				<div class="alert alert-dismissable alert-info">
					<h4>곧 시작됩니다!</h4>
					<p>시즌 시작전입니다. 등번호, 포지션을 수정할 수 있습니다!</p>
				</div>

			<%	}else if (league.status == 'playing') { %>
				<div class="alert alert-dismissable alert-info">
					<h4>시즌 중...</h4>
					<p>시즌이 진행되고 있습니다!</p>
				</div>

			<%	}else if (league.status == 'end') { %>
				<div class="alert alert-dismissable alert-info">
					<h4>시즌 종료!</h4>
				</div>
			<%	} %>