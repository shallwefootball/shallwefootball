<%	//joined League
	if (league.userJoined) {
%>

		<div class="jumbotron">
			<h1><%= user.playerName %>님 <br> 리그는 곧 시작합니다.</h1>
			<p><strong><%= league.community %> <%= league.season %>시즌</strong>에 <strong><%= user.teamName %></strong> 팀이 등록되었습니다.</p>
		</div>

<%	// Did Not joined
	}else {
%>

	<% if (user.createTeamName == null && user.clubId == null) { %>
		<div class="jumbotron">
			<h1>가입된 클럽이 없습니다!!</h1>
			<p>본인은 팀의 리더인가요?? 그럼 팀을 생성하세요.</p>
			<a class="btn btn-lg btn-block btn-success" data-toggle="modal" data-target="#createTeam" role="button">
				팀 만들기&nbsp;
				<span class="glyphicon glyphicon-ok" aria-hidden="true"></span>
			</a>
			<br>
			<p>선수로 본인을 등록하고 싶다면 아래에 참가팀에 가입하세요.&nbsp;
				<span class="glyphicon glyphicon-hand-down" aria-hidden="true"></span>
			</p>

		</div>

		<% include ../team/createTeamModal %>

	<% } %>


	<% if (user.createId && !user.teamId) { %>
		<div class="jumbotron">
			<h2> <strong><%= user.playerName %></strong> 님 <br> 클럽을 생성하셨군요!!</h2>
			<p>이제 리그에 팀을 등록해야 합니다. <strong><%= league.community %> <%= league.season %>시즌</strong>에 <strong><%= user.createTeamName %></strong> 팀을  등록하실건가요???</p>
			<a class="btn btn-lg btn-block btn-success" data-toggle="modal" data-target="#joinLeague" role="button">
				리그에 팀 등록&nbsp;
				<span class="glyphicon glyphicon-ok-sign" aria-hidden="true"></span>
			</a>
		</div>

		<% include ../club/joinLeagueModal %>

	<% } %>
<%	} %>




